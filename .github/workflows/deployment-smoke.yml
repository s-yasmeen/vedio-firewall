name: TAPF-MIN deployment smoke test

on:
  push:
    branches: [tapf-min-research]
    paths:
      - 'service/**'
      - 'tapf/**'
      - 'Dockerfile'
      - 'requirements*.txt'
      - 'tests/test_service.py'
      - '.github/workflows/deployment-smoke.yml'
  pull_request:
    branches: [main]

jobs:
  deployment-smoke:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deployment dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-deploy.txt
      - name: Run service tests
        run: pytest -q tests/test_service.py tests/test_controller.py tests/test_transforms.py
      - name: Build deployment image
        run: docker build -t tapf-min:test .
      - name: Start deployment image
        run: |
          docker run -d --name tapf-min-test -p 8080:8080 tapf-min:test
          sleep 8
      - name: Verify health endpoint
        run: |
          python - <<'PY'
          import json, urllib.request
          data=json.load(urllib.request.urlopen('http://127.0.0.1:8080/healthz', timeout=5))
          assert data['status']=='ok'
          assert data['fail_closed'] is True
          assert data['raw_biometric_ingestion'] is False
          print(data)
          PY
      - name: Container logs on failure
        if: failure()
        run: docker logs tapf-min-test || true
